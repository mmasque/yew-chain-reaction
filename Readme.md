# Chain reaction
A Rust implementation of the chain reaction game.
Player mechanics not added, so you can't die, and there are always four players.
The only way to win is to have all the squares.
Run locally with `trunk serve --open`.